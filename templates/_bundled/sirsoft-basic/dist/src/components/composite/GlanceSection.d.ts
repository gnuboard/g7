import { default as React } from 'react';
/**
 * S6 — 한 눈에 본, AICT (At A Glance).
 * 3개 블록: 경영 성과(카드) / 논문·특허+ESG 경영(2-col) / 기술이전·창업(숫자).
 */
declare const GlanceSection: React.FC;
export default GlanceSection;
